"""Согласие пользователя на уведомления по электронной почте.

П. 4.2.11 ТЗ: уведомления направляются двумя каналами — в интерфейсе системы
и по электронной почте, причём почтой «только при согласии пользователя на
рассылку (38-ФЗ), с возможностью отказа в настройках». Поля, хранящего это
согласие, в схеме версии 2 не было.

Значение по умолчанию — включено. Обоснование: уведомления касаются
исключительно программ, которые пользователь сам добавил в избранное либо по
которым ведёт заявку, и сообщают о приближении срока подачи. Это не
рекламная рассылка, а сервисные сообщения по инициированным пользователем
действиям. Отказ доступен в настройках в один переключатель, что и требует
ТЗ формулировкой «с возможностью отказа».

Уведомления в интерфейсе системы флагом не управляются: они не покидают
систему и под требование о рассылке не подпадают.

Revision ID: 0010
Revises: 0009
"""

from alembic import op

revision = "0010"
down_revision = "0009"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.execute(
        "ALTER TABLE app_user ADD COLUMN email_notifications BOOLEAN NOT NULL DEFAULT TRUE"
    )
    op.execute(
        "COMMENT ON COLUMN app_user.email_notifications IS "
        "'Согласие на получение уведомлений по электронной почте (38-ФЗ, FR-011); "
        "отключается пользователем в настройках'"
    )


def downgrade() -> None:
    op.execute("ALTER TABLE app_user DROP COLUMN email_notifications")
