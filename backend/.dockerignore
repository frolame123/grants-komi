# Виртуальное окружение разработки в образ не переносится: зависимости
# ставятся внутри контейнера под его архитектуру
.venv/
venv/
__pycache__/
*.py[cod]

# Локальные настройки с паролями и секретами
.env

# Собранный набор сертификатов — восстанавливается при первом запросе
certs/bundle.pem
